export default class Tasks {
    constructor(
        taskId,
        task,
        taskMarked,
    ) {
        this.taskId = taskId;
        this.task = task;
        this.taskMarked = taskMarked;
    }
}